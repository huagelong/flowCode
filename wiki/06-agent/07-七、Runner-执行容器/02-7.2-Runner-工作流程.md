> 来源：`docs/plan/06-agent.md` 第 519 行
> 位置：[总目录](../../README.md) -> [AnserFlow - anserAgent 智能体系统](../README.md) -> [七、Runner 执行容器](README.md) -> 7.2 Runner 工作流程
> 相邻：[上一篇](01-7.1-Runner-职责.md) · [下一篇](03-7.3-与现有模块的关系.md)
> 相关主题：[返回上级章节](README.md) · [返回文档入口](../README.md)

### 7.2 Runner 工作流程

```
Runner.Query(input, checkPointID)
    │
    ├─► 1. 创建/加载 CheckPoint
    ├─► 2. Agent.Run(ctx, input) → AsyncIterator
    │       │
    │       ├─► AgentEvent{Output}   → 流式推送给调用方
    │       ├─► AgentEvent{Action}   → 记录 Token 用量
    │       └─► AgentEvent{Interrupted} → 保存 CheckPoint → 返回控制权给调用方
    │
    └─► 3. 完成后删除 CheckPoint

Runner.Resume(checkPointID, params)
    │
    ├─► 1. 加载 CheckPoint
    ├─► 2. 恢复执行上下文
    ├─► 3. 将 ResumeParams 注入中断点
    └─► 4. Agent.Run() 从中断点继续
```
