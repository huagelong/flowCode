# Admin 审计日志

> 路由：`/admin/audit-logs`
> API：`GET /api/audit-logs`
> 权限：仅 `super_admin`

---

## 1. 页面布局

```
┌──────────────────────────────────────────────────────────────────┐
│ 审计日志                                                        │
│                                                                  │
│ 筛选: [组织 ▾] [操作类型 ▾] [资源类型 ▾]                        │
│ 时间范围: [开始日期] ~ [结束日期]                                │
│                                                  [导出 CSV]     │
│                                                                  │
│ ┌────────────────────────────────────────────────────────────┐  │
│ │ 时间       │ 操作者  │ 操作       │ 资源     │ IP      │ 详情│  │
│ ├────────────────────────────────────────────────────────────┤  │
│ │ 05-20 14:30│ 张三   │ update     │ agent    │ 10.0...│ ⋯  │  │
│ │ 05-20 12:15│ 系统   │ status_chg │ issue    │ —      │ ⋯  │  │
│ │ 05-19 18:00│ 李四   │ delete     │ member   │ 10.0...│ ⋯  │  │
│ │ 05-19 10:30│ 张三   │ create     │ project  │ 10.0...│ ⋯  │  │
│ │ 05-18 09:00│ 系统   │ invite     │ member   │ —      │ ⋯  │  │
│ └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│ ← 1 2 3 ... 50 →                                                │
└──────────────────────────────────────────────────────────────────┘
```

---

## 2. 筛选器

### 组织筛选

```
┌──────────────────────────┐
│ 全部组织                ▾│
│ My Organization          │
│ Acme Corp                │
│ Team Alpha               │
└──────────────────────────┘
```

- Select 下拉，列出所有组织
- 默认"全部组织"

### 操作类型筛选

| 操作 | 说明 |
|------|------|
| create | 创建 |
| update | 更新 |
| delete | 删除 |
| invite | 邀请 |
| status_change | 状态变更 |
| enable | 启用 |
| disable | 禁用 |

### 资源类型筛选

| 资源 | 说明 |
|------|------|
| organization | 组织 |
| member | 成员 |
| project | 项目 |
| issue | Issue |
| agent | Agent |
| skill | Skill |
| runtime | Runtime |
| setting | 系统设置 |

### 时间范围

- 开始日期 / 结束日期 Date Picker
- 快捷选项：今天 / 最近7天 / 最近30天 / 自定义
- 默认：最近7天

---

## 3. 表格列

| 列 | 字段 | 宽度 | 说明 |
|------|------|------|------|
| 时间 | `created_at` | 140px | `YYYY-MM-DD HH:mm` |
| 操作者 | `user_id` → username | 120px | Avatar (24px) + 名称，系统操作显示 "系统" |
| 操作 | `action` | 120px | Badge 彩色标签 |
| 资源类型 | `resource_type` | 100px | Badge |
| 资源ID | `resource_id` | 80px | 可点击跳转 |
| IP | `ip` | 120px | 系统操作显示 "—" |
| 详情 | `detail` JSON | 60px | ⋯ 查看详情 |

### 操作 Badge 颜色

| 操作 | 颜色 |
|------|------|
| create | `bg-green-50 text-green-700` |
| update | `bg-blue-50 text-blue-700` |
| delete | `bg-red-50 text-red-700` |
| invite | `bg-purple-50 text-purple-700` |
| status_change | `bg-amber-50 text-amber-700` |
| enable | `bg-green-50 text-green-700` |
| disable | `bg-zinc-50 text-zinc-700` |

---

## 4. 详情 Dialog

点击 ⋯ 按钮展开详情：

```
┌──────────────────────────────────────────────────────────────┐
│ 审计详情                                                   ✕  │
│ ──────────────────────────────────────────────────────────── │
│                                                              │
│ 时间    2026-05-20 14:30:15                                 │
│ 操作者  张三 (user_id: 1)                                   │
│ 操作    update                                              │
│ 资源    agent (id: 3)                                       │
│ IP      10.0.1.42                                           │
│                                                              │
│ 变更详情                                                     │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ {                                                        │ │
│ │   "field": "runtime_config.model",                       │ │
│ │   "old_value": "gpt-4o",                                 │ │
│ │   "new_value": "gpt-4o-mini"                             │ │
│ │ }                                                        │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

- `detail` JSON 使用 JSON 格式化展示
- 等宽字体 `font-mono text-sm`
- 暗色背景代码块 `bg-zinc-900 text-zinc-100 rounded-lg p-3`
- 语法高亮（JSON keys 蓝色，字符串值绿色，数字橙色）
- 自动缩进 2 空格
- 最大高度 `max-h-[300px] overflow-y-auto`
- 右上角 [📋 复制] 按钮（复制格式化 JSON）
- 嵌套对象默认折叠，点击 `▶` 展开

---

## 5. 导出 CSV

- 导出当前筛选结果
- 列：时间, 操作者, 操作, 资源类型, 资源ID, IP, 详情
- 文件名：`audit-logs-{org_name}-{date}.csv`
- 最多导出 10000 条
- 超过 10000 条时 Toast 提示 "当前筛选结果超过 10000 条，仅导出前 10000 条"
- CSV 格式：UTF-8 with BOM（兼容 Excel 中文）
- 日期格式：`YYYY-MM-DD HH:mm:ss`
- 详情列：JSON 压缩为单行字符串
- 分隔符：逗号，文本字段用双引号包裹

---

## 6. 空状态

```
        [📋 图标 64px]
     暂无审计日志
 调整筛选条件查看更多记录
```

---

## 7. 数据加载

- 分页：每页 50 条
- 排序：默认按时间降序
- 筛选：服务端过滤（所有筛选参数传给后端）
