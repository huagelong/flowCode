# Admin 群组 / 会话管理

> 路由：`/admin/groups`
> API：`/api/groups/*`, `/api/orgs/:org_id/conversations`

---

## 1. 会话列表页

```
┌──────────────────────────────────────────────────────────────────┐
│ 会话管理                                    [+ 创建群组]        │
│                                                                  │
│ 筛选 [类型 ▾] [关联项目 ▾]                                      │
│                                                                  │
│ ┌────────────────────────────────────────────────────────────┐  │
│ │ 类型      │ 名称        │ 关联项目   │ 成员    │ 创建时间 │  │
│ ├────────────────────────────────────────────────────────────┤  │
│ │ [group]   │ 项目讨论组  │ anserFlow │ 👤2 🤖3│ 05-15   │  │
│ │ [direct]  │ —           │ —         │ 👤1 🤖1│ 05-20   │  │
│ │ [group]   │ 技术评审    │ mobile    │ 👤3 🤖1│ 05-18   │  │
│ └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│ ← 1 2 →                                                         │
└──────────────────────────────────────────────────────────────────┘
```

### 类型 Badge

| 类型 | 样式 | 文字 |
|------|------|------|
| group | `bg-blue-50 text-blue-700` | "群组" |
| direct | `bg-green-50 text-green-700` | "私信" |

### 表格列

| 列 | 宽度 | 说明 |
|------|------|------|
| 类型 | 80px | group/direct Badge |
| 名称 | flex | group 显示名称，direct 显示 "—" |
| 关联项目 | 120px | 关联的项目名，无则为 "—" |
| 成员 | 120px | 用户数 + Agent 数 |
| 创建时间 | 100px | 格式化日期 |
| 操作 | 60px | 查看详情 / 删除 |

---

## 2. 创建群组 Dialog

```
┌──────────────────────────────────────────────────────────────┐
│ 创建群组                                                  ✕  │
│ ──────────────────────────────────────────────────────────── │
│                                                              │
│ 类型 *                                                       │
│ ○ 群组讨论   ○ 私信                                          │
│                                                              │
│ 群组名称 * (群组类型必填)                                     │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 项目讨论组                                               │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                              │
│ 关联项目                                                     │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ anserFlow                                              ▾ │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                              │
│ 成员                                                         │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 👤 张三  ✕  │ 👤 李四  ✕  │ 🤖 CEO  ✕  │ + 添加       │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                              │
│                                         [取消] [创建]       │
└──────────────────────────────────────────────────────────────┘
```

### 成员选择器

- Combobox 支持搜索用户和 Agent
- 已选成员显示为 Tag (Avatar + 名称 + × 删除)
- Agent 带 Bot 图标区分
- 私信类型：只能选择 1 人（用户或 Agent），无需名称

---

## 3. 群组详情页

路由：`/admin/groups/:group_id`

```
┌──────────────────────────────────────────────────────────────┐
│ [← 返回]  项目讨论组                                         │
│            群组 · anserFlow · 👤2 🤖3 · 创建于 05-15         │
│                                                              │
│ [成员]  [消息记录]                                           │
│                                                              │
│ ┌─── 成员 Tab ──────────────────────────────────────────────┐│
│ │                                            [+ 添加成员]  ││
│ │                                                          ││
│ │ 👤 张三     (创建者)                                     ││
│ │ 👤 李四                                               🗑 ││
│ │ 🤖 CEO        CEO · anserAgent                          ││
│ │ 🤖 前端Agent  Frontend · anserAgent                     ││
│ │ 🤖 后端Agent  Backend · anserAgent                      ││
│ └──────────────────────────────────────────────────────────┘│
│                                                              │
│ ┌─── 消息记录 Tab ─────────────────────────────────────────┐│
│ │ (只读消息列表，不可发送)                                  ││
│ │                                                          ││
│ │ 搜索 [🔍...]           筛选 [发送者 ▾]                   ││
│ │                                                          ││
│ │ 14:30  👤 张三                                           ││
│ │        我们需要优化登录页面的验证逻辑                      ││
│ │        ──────                                            ││
│ │ 14:31  🤖 CTO                                            ││
│ │        同意。建议拆分为以下 Issue...                      ││
│ │        ──────                                            ││
│ │ 14:35  👤 张三                                           ││
│ │        帮我拆解一下这个需求                              ││
│ │        ──────                                            ││
│ │ 14:36  📋 方案确认卡片                                   ││
│ │        ┌────────────────────────────────────────────┐    ││
│ │        │ #42 修复登录页面验证逻辑 P1 · 🤖前端Agent  │    ││
│ │        │                   [拒绝] [确认转 Todo]      │    ││
│ │        └────────────────────────────────────────────┘    ││
│ │                                                          ││
│ │ ← 1 2 3 →                                               ││
│ └──────────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────┘
```

### 消息气泡样式 (只读)

| 发送者 | 对齐 | 头像位置 | 样式 |
|--------|------|----------|------|
| 用户 | 左 | 左侧 | `bg-muted rounded-lg px-3 py-2` |
| Agent | 左 | 左侧 + Bot 图标 | `bg-card border rounded-lg px-3 py-2` |
| Agent 成果卡片 | 左 | 🤖 图标 | AgentOutputCard 结构化卡片（见下） |
| 系统 | 居中 | 无 | `bg-muted/50 text-muted-foreground text-xs italic rounded px-3 py-1` |
| 方案确认卡片 | 左 | 📋 图标 | 交互式卡片（见下） |

### Agent 成果卡片 (AgentOutputCard)

Agent 消息中的结构化成果自动渲染为对应类型的 AgentOutputCard（与 Client 聊天窗口共享同一组件）：

| 卡片类型 | 触发时机 | 渲染方式 |
|----------|----------|----------|
| 文件生成 | Agent 生成/修改文件 | 文件列表 + 可展开 DiffViewer |
| Commit | Agent 提交代码 | 提交信息 + 哈希 + 文件变更统计 |
| PR 创建 | Agent 创建 PR | PR 标题 + source→target + 外部链接 |
| 测试通过 | Agent 运行测试通过 | 绿色卡片 + 用例列表 |
| 测试失败 | Agent 测试有失败 | 红色卡片 + 失败详情 + 堆栈 |
| 执行错误 | Agent 执行出错 | 错误信息 + 堆栈 + 重试按钮 |

**渲染规则**:
- Agent 消息中的普通文字用 Markdown 只读渲染
- 结构化成果通过 WebSocket `agent_log` 事件中的 `action` 字段区分卡片类型
- 卡片完整样式规范见 [components/08-through-13.md](../components/08-through-13.md) AgentOutputCard 部分
- DiffViewer 同样共享，支持暗色/亮色主题
- 所有卡片为**只读模式**（Admin 端无重试、无确认操作，仅展示）

### PR 审核卡片 (review_decision)

Admin 端群组消息列表中也渲染交互式 PR 审核卡片（与 Client 端共享同一组件）。该卡片仅在 Issue 进入 `in_review` 时出现。

**交互能力**:
- [审核通过] — 调用 `review_decision` WebSocket 消息 → 等待 PR merge 后 Issue 变为 `done`
- [退回修改] — Issue 回到 `todo`，保留 PR 与执行上下文
- 决策后卡片变为只读态

**权限**: 所有有 Issue 管理权限的成员 (owner/admin) 均可操作。

### 添加成员

- 群组类型：可添加多个用户/Agent
- 私信类型：不可添加（返回 400）
- 添加后通过 WebSocket 广播成员变更

---

## 4. Admin 端消息管理特点

- **只读**: Admin 端不提供发送消息功能（发送在 Client 端完成）
- **可搜索**: 按内容关键词搜索
- **可筛选**: 按发送者类型（用户/Agent/系统）
- **分页**: 每页 50 条，按时间倒序
