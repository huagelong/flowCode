> 来源：`docs/plan/04-sandbox.md` 第 1467 行
> 位置：[总目录](../../README.md) -> [AnserFlow - 沙箱 / Agent 基础设施](../README.md) -> [二、Docker 沙箱方案](README.md) -> 2.1 执行流程
> 相邻：[上一篇](01-2.0-容器与代码隔离策略.md) · [下一篇](03-2.1.1-沙箱-↔-系统消息互通闭环.md)
> 相关主题：[返回上级章节](README.md) · [返回文档入口](../README.md)

### 2.1 执行流程

每个 Issue 在**项目常驻容器**内通过**独立 git worktree** 执行，多 Issue 并发互不干扰：

```

┌──────────────────────────────────────────────────────────┐

│  Asynq Worker                                             │

│  ┌────────────────────────────────────────────────────┐  │

│  │  Step 1: 准备                                      │  │

│  │  ├── 读取 Issue 上下文                             │  │

│  │  ├── 加载 Agent 配置                               │  │

│  │  └── 加载绑定的 Skills                             │  │

│  │                                                    │  │

│  │  Step 2: 确保项目容器存活（常驻）                     │  │

│  │  ├── 查询 projects.sandbox_container_id             │  │

│  │  ├── 容器存在且运行中 → 直接复用                    │  │

│  │  ├── 容器不存在 → ContainerCreate + Start           │  │

│  │  │   ├── Image: anserflow/sandbox                  │  │

│  │  │   ├── Memory: 1GB, CPU: 2 cores                │  │

│  │  │   ├── NamedVol: workspace-{projectID}:/workspace│  │

│  │  │   ├── BindMount: project-runtime→home (rw)     │  │

│  │  │   └── AutoRemove: false (常驻)                  │  │

│  │  └── 首次: git clone 到 /workspace/main            │  │

│  │                                                    │  │

│  │  Step 3: 创建 Issue 专属 worktree                  │  │

│  │  ├── docker exec git worktree add \               │  │

│  │  │     /workspace/issue-{id} -b feat/issue-{id}    │  │

│  │  ├── 分支名: feat/issue-{id}                       │  │

│  │  └── 工作目录: /workspace/issue-{id}（物理隔离）    │  │

│  │                                                    │  │

│  │  Step 4: 注入运行时配置                             │  │

│  │  ├── 读取 Agent runtime_config                      │  │

│  │  ├── 解密 API Key → 环境变量                        │  │

│  │  ├── 写入 config.json 到容器                        │  │

│  │  ├── 写入 Runtime 默认 Skills                       │  │

│  │  └── 写入 Agent 绑定 Skills                         │  │

│  │                                                    │  │

│  │  Step 5: 在 worktree 内执行 anserAgent             │  │

│  │  │  docker exec \                                  │  │

│  │  │    --workdir /workspace/issue-{id} \            │  │

│  │  │    anserflow agent run --prompt "实现登录页"     │  │

│  │  │                                                  │  │

│  │  │  ┌─ anserAgent exec ←→ Worker 消息环 ─────────┐  │  │

│  │  │  │  anserAgent 通过 stdout JSON Lines 实时输出  │  │  │

│  │  │  │  Worker 解析 → DB + WebSocket → 前端       │  │  │

│  │  │  └──────────────────────────────────────────┘  │  │

│  │  │                                                  │  │

│  │  ├── 通过 → git push + 创建 PR → 进入 in_review         │  │

│  │  └── 失败 → 写时间线 → Issue → todo                   │  │

│  │                                                    │  │

│  │  Step 6: GitHub PR 审核 + 合并                      │  │

│  │  ├── anserAgent 检查通过:                            │  │

│  │  │   ├── git add → commit（在 worktree 内）        │  │

│  │  │   ├── git push + 创建 Pull Request              │  │

│  │  │   │   ├── Worker 写入 PR URL + 质量结果         │  │

│  │  │   │   └── Issue 状态 → in_review                │  │

│  │  │   ├── 自然人在 GitHub 审核并合并 PR             │  │

│  │  │   │   ├── Webhook 通知 PR merged                │  │

│  │  │   │   ├── 更新 Issue → done                     │  │

│  │  │   │   └── git worktree remove + branch -D       │  │

│  │  │   └── PR 被拒绝/关闭未合并 → Issue → todo       │

│  │  ├── anserAgent 检查失败:                            │  │

│  │  │   ├── 写入失败原因到时间线                       │  │

│  │  │   ├── Issue → todo（保留 worktree 待重试）      │  │

│  │  │   └── 等待人工提示词 → 重试                     │  │

│  │  └── 项目容器不销毁（常驻，给其他 Issue 复用）      │  │

│  └────────────────────────────────────────────────────┘  │

│  Step 7: 容器崩溃恢复                               │  │
│  ├── Worker 检测容器状态（每次调度前查询 Docker API） │  │
│  ├── 容器不存在或已停止 → 记录告警                    │  │
│  ├── 重建容器（复用 NamedVolume，数据不丢失）         │  │
│  ├── 恢复 project.sandbox_container_id               │  │
│  └── 正在执行的 Issue → todo（保留 worktree 待重试） │  │
│                                                    │  │
│  Step 8: Worktree 生命周期与清理                    │  │
│  ├── 正常结束: Issue done → git worktree remove    │  │
│  ├── 异常保留: 执行失败/PR被拒/人工停止 → 保留 14 天  │  │
│  ├── 定时清理: 每日凌晨扫描 worktree 创建时间         │  │
│  │   └── 超过 14 天的孤立 worktree → git worktree remove + branch -D│
│  ├── 项目归档: 项目归档/删除时清理全部关联 worktree   │  │
│  ├── 存储告警: /workspace 占用超过 80% → 群聊通知    │  │
│  └── 手动清理: 管理后台可查看/删除孤立的 worktree     │  │

└──────────────────────────────────────────────────────────┘

```
